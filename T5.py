""" T5. Make abstract class `Shape` which has abstract methods `perimiter()` and `area()`.
    Make classes `Circle`, `Rectangle` and `Square` with required attributes and implement
    abstract method inherited from `Shape` class."""

